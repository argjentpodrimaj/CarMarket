<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acer.carmarketfinal">
	
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

     <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RegisterForm" />
        <activity android:name=".activities.KrijoShpalljeForm" />
        <activity android:name=".activities.ListViewCars" />
        <activity android:name=".activities.LoginForm" />
        <activity android:name=".activities.DetajeVetura" />
        <activity android:name=".activities.AccountSetting" />
        <activity android:name=".activities.ListUser"></activity>
    </application>

</manifest>